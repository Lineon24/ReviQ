OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
# Required on Vercel. Use a long random password for your private workspace.
APP_PASSWORD=
# Optional local Python API port (use the same value when building and starting).
REVIQ_API_PORT=8000
